# Addidtional Documentation

## icon: string (byte) 
Any icon in the documentation with the schema `string (byte)` is a base64 encoded svg file. Create a base64-string based on a `icon.svg` using the terminal (mac/linux): `$ base64 icon.svg` 